# config file format version (required)
version: 2

# build the documentation with Sphinx from the source/ directory
sphinx:
  configuration: source/conf.py

python:
  version: 3.8
  install:
    - requirements: requirements.txt
